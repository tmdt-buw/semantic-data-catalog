name: Docker Hub Backend

on:
  push:
    branches: ["main"]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN_BACKEND }}
      - uses: docker/build-push-action@v6
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: son1i/semantic-data-catalog-backend:latest
      - uses: docker/build-push-action@v6
        with:
          context: ./fuseki
          file: ./fuseki/Dockerfile
          push: true
          tags: |
            son1i/semantic-data-catalog-fuseki:6.2.0
            son1i/semantic-data-catalog-fuseki:latest
